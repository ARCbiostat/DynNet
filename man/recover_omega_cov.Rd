% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recover_omega_cov.R
\name{recover_omega_cov}
\alias{recover_omega_cov}
\title{Recover a single Omega-level covariance matrix from a single alternating Lambda-level matrix}
\usage{
recover_omega_cov(
  Blambda,
  Blambda_fixed,
  mappingL1L2,
  method = c("structured", "pseudoinverse"),
  rho_int0 = 0,
  rho_int = 0,
  rho_slope = 0
)
}
\arguments{
\item{Blambda}{(nD*nTypes) x (nD*nTypes) Lambda-level var/cov (non-Cholesky).
Rows/cols alternate by type: int0, int, slope (or int0, int).}

\item{mappingL1L2}{nD x nL; (d,l) is the weight ω_{d,l} of Omega l in Lambda d.}

\item{method}{"structured" (default) or "pseudoinverse".}

\item{rho_int0}{scalar or length nD vector: intra-Ω correlation for INT0 within Λ_d group.}

\item{rho_int}{scalar or length nD vector: intra-Ω correlation for INT within Λ_d group.}

\item{rho_slope}{scalar or length nD vector: intra-Ω correlation for SLOPE within Λ_d group.
Ignored if slopes absent.}

\item{intercept_var_fixed}{if TRUE, force Var(Λ_d intercept)=1 in the INT block.}
}
\value{
A single Omega-level covariance matrix BOmega of size (nL + 2*nL) x (nL + 2*nL)
        ordered as: [all Ω int0] then [Ω1(int), Ω1(slope), Ω2(int), Ω2(slope), ...].
        Attributes include a list of components for inspection.
}
\description{
Recover a single Omega-level covariance matrix from a single alternating Lambda-level matrix
}
