% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_matrix_nL.R
\name{model_matrix_nL}
\alias{model_matrix_nL}
\title{Repeat LP-specific columns in a data frame}
\usage{
model_matrix_nL(df, repeats_lp, add_suffix = TRUE, mod = 1)
}
\arguments{
\item{df}{A \code{data.frame} containing columns named with LP patterns (e.g., "DeltaLP.<id>.<var>").}

\item{repeats_lp}{A named integer vector where colnames are LP IDs (as character) and values are the number of repetitions.
LPs not listed will default to 1 repetition.}

\item{add_suffix}{Logical; if \code{TRUE}, adds suffixes like "__rep1", "__rep2" to repeated columns for uniqueness.}

\item{pattern_fun}{Optional function to extract LP IDs from column colnames. Defaults to regex for "DeltaLP.<id>.".}
}
\value{
A \code{data.frame} with repeated columns according to \code{repeats_lp}.
}
\description{
Creates a new data frame by repeating groups of columns corresponding to LP identifiers
(e.g., "DeltaLP.1.", "DeltaLP.2.") a specified number of times for each LP.
}
\details{
Columns are grouped by LP ID based on their colnames. Non-LP columns are kept once by default.
The function preserves the original column order and repeats LP blocks contiguously.
}
\examples{
df <- data.frame(
  `DeltaLP.1.(Intercept)` = rnorm(4),
  `DeltaLP.1.Time`        = rnorm(4),
  `DeltaLP.2.(Intercept)` = rnorm(4),
  `DeltaLP.2.Time`        = rnorm(4),
  check.colnames = FALSE
)

# Repeat LP 1 three times, LP 2 twice
repeats_lp <- c("1" = 3, "2" = 2)
df_new <- repeat_lp_columns(df, repeats_lp, add_suffix = TRUE)
colnames(df_new)

}
